this program has a class that takes a randomly generated number and a password. 
It concatinates the two and returns a byte array of the pair hased with SHA512.
The program class gathers the password and username and generates the pass key, 
then writes them to a database table